#!/usr/bin/env node
import { NAME, VERSION } from './index.js';

// stdio MCP server entry point.
// Server, session, debug agent, and adapters land incrementally — see idea/.
console.error(`${NAME} v${VERSION}: not implemented yet. See idea/ for the design.`);
process.exit(1);
